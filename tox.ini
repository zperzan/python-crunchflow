[tox]
requires =
    tox>=4
    tox-uv>=1
envlist = py{39,310,311,312,313}

[testenv]
description = Run default tests with pytest via uv
runner = uv-venv-lock-runner
package = editable
dependency_groups = test
commands =
    pytest tests/ --import-mode importlib